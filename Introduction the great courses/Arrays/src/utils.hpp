/**
 * Header file for all the constant and functions needed in this program
 */

#ifndef UTILS
#define UTILS
#include <string>
#include <iostream>
#include <vector>
#include <limits>

extern std::string days[];
extern int readNumberOfDays();
extern std::vector<float> avgTemp(int);
extern int getIndex(int i, int size);
extern int xdArrays();
#endif
