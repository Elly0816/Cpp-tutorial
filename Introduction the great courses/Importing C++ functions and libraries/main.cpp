// This function is the main entry function
// #include <cmath>
// #include <iostream>
// #include <cstdlib>
// #include <ctime>
#include "utils.hpp"

using namespace std;

int main()
{

    guessingGame();
    // srand(time(0)); // This sets the seed for a random number generator, the time function returns the current clock time
    // int x = (rand() % 100) + 1;
    // // cout<<"The random number generated is: "<<rand()<<endl;
    // // cout<<"The random number generated is: "<<rand()<<endl;
    // cout << "The random number generated is: " << x << endl;
    // double value =  2.5;

    // cout<<"The value of square root "<<value<<" is: "<<sqrt(2.5)<<endl;

    return 0;
}